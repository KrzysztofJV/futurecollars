package com.futurecollars.taskSeven;

public interface TrainingResult {

    int calculateLengthParameter(int trainingTime);
    int calculateBurnedParameter(int burnedCalories);
    int calculatePulseParameter(int puls);
}
